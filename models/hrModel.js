**Rezultat scanare linii markdown: 0 linii markdown detectate.**

Fișierul `gastrohub-core/models/hrModel.js` (1422 linii) este un model JavaScript pentru operațiuni HR (pontaj și salarii). Conține exclusiv:
- Comentarii JSDoc (`/** ... */`) — documentație standard JavaScript, NU markdown
- Cod JavaScript
- Interogări SQL
- Separatori tip `// ----`

Liniile care încep cu `*` sunt continuări ale blocurilor JSDoc, nu elemente de markdown. Nu există headere (`#`), linkuri (`[]()`), blocuri de cod markdown (```), citate (`>`), tabele (`|`), bold (`**`), sau liste markdown în acest fișier.